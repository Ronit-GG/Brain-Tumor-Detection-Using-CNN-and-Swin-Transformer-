# =====================================================================
# Brain Tumor Detection — Pinned dependency manifest
# Target: Python 3.11.x on Windows 10/11 with NVIDIA GPU (CUDA 12.6).
#
# Install order (see setup.ps1):
#   1) torch / torchvision  -> from PyTorch CUDA wheel index
#   2) everything below     -> from PyPI
# =====================================================================

# --- Deep learning core (installed separately from CUDA index) -------
# torch==2.12.0           # installed via: pip install torch ... --index-url ...
# torchvision==0.27.0
# torchaudio==2.12.0

# --- Pretrained model zoo --------------------------------------------
timm==1.0.27

# --- Numerical / data ------------------------------------------------
# (numpy/pillow are installed transitively by torchvision; leave bounds loose)
numpy>=1.26
pandas>=2.2

# --- Image I/O + processing ------------------------------------------
opencv-python>=4.10
Pillow>=10.4

# --- ML utilities ----------------------------------------------------
scikit-learn>=1.5,<1.7

# --- Visualization ---------------------------------------------------
matplotlib>=3.9,<3.11
seaborn>=0.13,<0.15

# --- Training UX -----------------------------------------------------
tqdm>=4.66
tensorboard>=2.18,<2.21

# --- Explainability --------------------------------------------------
grad-cam>=1.5,<1.7

# --- Frontend --------------------------------------------------------
streamlit>=1.40,<2.0

# --- API backend (optional, used by app/api.py) -----------------------
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# --- Notebooks -------------------------------------------------------
jupyter>=1.1
ipykernel>=6.29
notebook>=7.2

# --- Optional: deployment / export -----------------------------------
onnx>=1.17
onnxruntime>=1.20

# --- Misc ------------------------------------------------------------
PyYAML>=6.0
